#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    ll x;
    cin >> x;
    if (x >= 3 && x <= 18) cout << "Yes";
    else cout << "No";
    return 0;
}
